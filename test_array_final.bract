fn main() {
    // Test basic array literals
    let numbers = [1, 2, 3, 4, 5];
    let floats = [1.0, 2.5, 3.14];
    
    // Test array indexing
    let first = numbers[0];
    let second = floats[1];
    
    // Test arrays in expressions
    let sum = numbers[0] + numbers[1];
} 